image: trion/ng-cli-karma:latest

before_script:
    - echo start before_script...

cache:
  paths:
    - webapp/node_modules/

stages:
  - build
  - test

job_build:
  stage: build
  script:
    - echo start job_build...
    - pwd
    - cd webapp
    - npm install
    - npm run env
    - npm run build

job_lint:
  stage: test
  script:
    - echo start job_lint...
    - pwd
    - npm run lint

job_karma:
  stage: test
  script:
    - echo start job_karma...
    - pwd
    - npm run test
